import { NomDeLaPipePipe } from './nom-de-la-pipe-pipe';

describe('NomDeLaPipePipe', () => {
  it('create an instance', () => {
    const pipe = new NomDeLaPipePipe();
    expect(pipe).toBeTruthy();
  });
});
